[project]
name = "strava-mcp"
version = "0.1.0"
description = "A simple MCP for Strava."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "httpx>=0.28.1",
    "mcp[cli]>=1.6.0",
    "meteostat>=1.6.8",
    "pre-commit>=4.2.0",
    "python-dotenv>=1.1.0",
]
license = { file = "LICENSE" }
authors = [{ name = "Jonathan Carlton" }]

[tool.ruff]
line-length = 79
